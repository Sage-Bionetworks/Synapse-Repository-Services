package org.sagebionetworks.web.unitclient;

import org.junit.Test;

public class PortalGinModuleTest {
	
	@Test
	public void testPortalGinModule(){
//		Injector injector = Guice.createInjector(new PortalGinModule());
//		Ginjector
//
//		GinBinder mockBinder = createMock(GinBinder.class);
//		BinderAdapter adapter = new BinderAdapter();
////		GinAnnotatedBindingBuilder<AllDatasetsViewImpl> allDatasetBinder =  createMock(GinAnnotatedBindingBuilder.class);
//////		expect(allDatasetBinder.in(Singleton.class));
////		
////		expect(mockBinder.bind(AllDatasetsViewImpl.class)).andReturn(allDatasetBinder);
//		PortalGinModule module = new PortalGinModule();
//		module.configure(mockBinder);
		
		
	}

}
