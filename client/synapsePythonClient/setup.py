#!/usr/bin/env python
from setuptools import setup

setup(
    name = 'synapsePythonClient',
    version = '0.1.0',
    author = 'platform',
    author_email = 'platform@sagebase.org',
    description = 'Python API to Synapse platform',
    packages = ['synapse']
#    install_requires = ['progressbar>=2.2']
)
