CREATE TABLE IF NOT EXISTS `UNSUCCESSFUL_LOGIN_LOCKOUT`(
  `USER_ID` BIGINT NOT NULL,
  `UNSUCCESSFUL_LOGIN_COUNT` BIGINT NOT NULL,
  `LOCKOUT_EXPIRATION` BIGINT NOT NULL,
  PRIMARY KEY (`USER_ID`)
);