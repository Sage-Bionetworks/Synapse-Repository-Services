CREATE TABLE `EVALUATION_ACCESS_REQUIREMENT` (
  `EVALUATION_ID` bigint(20) NOT NULL,
  `REQUIREMENT_ID` bigint(20) NOT NULL,
  PRIMARY KEY (`EVALUATION_ID`, `REQUIREMENT_ID`),
  CONSTRAINT `EVALUATION_ACCESS_REQUIREMENT_EVALUATION_ID_FK` FOREIGN KEY (`EVALUATION_ID`) REFERENCES `JDOEVALUATION` (`ID`) ON DELETE CASCADE,
  CONSTRAINT `EVALUATION_ACCESS_REQUIREMENT_REQUIREMENT_ID_FK` FOREIGN KEY (`REQUIREMENT_ID`) REFERENCES `ACCESS_REQUIREMENT` (`ID`) ON DELETE CASCADE
)