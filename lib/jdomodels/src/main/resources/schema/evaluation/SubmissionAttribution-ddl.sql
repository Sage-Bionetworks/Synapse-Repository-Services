CREATE TABLE ATTRIBUTION (
    ID bigint(20) NOT NULL,
    SUBMISSION_ID bigint(20) NOT NULL,
    ADDED_ON bigint(20) NOT NULL,
    ATTRIBUTION mediumblob,
    PRIMARY KEY (ID),
    FOREIGN KEY (SUMISSION_FK) REFERENCES JDOSUBMISSION (ID) ON DELETE CASCADE
);