CREATE TABLE `jdoresourceaccess` (
  `ID` bigint(20) NOT NULL AUTO_INCREMENT,
  `OWNER_ID` bigint(20) DEFAULT NULL,
  `GROUP_ID` bigint(20) NOT NULL,
  PRIMARY KEY (`ID`),
  KEY `JDORESOURCEACCESS_N49` (`OWNER_ID`),
  CONSTRAINT `RESOURCE_ACCESS_OWNER_FK` FOREIGN KEY (`OWNER_ID`) REFERENCES `acl` (`ID`) ON DELETE CASCADE
)