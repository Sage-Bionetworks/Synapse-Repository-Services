CREATE TABLE IF NOT EXISTS `JDODATEANNOTATION` (
  `ID` bigint(20) NOT NULL AUTO_INCREMENT,
  `ATTRIBUTE` varchar(256) CHARACTER SET latin1 COLLATE latin1_bin DEFAULT NULL,
  `OWNER_ID` bigint(20) NOT NULL,
  `VALUE` datetime DEFAULT NULL,
  PRIMARY KEY (`ID`),
  UNIQUE KEY `DATE_ANNO_UNIQUE` (`OWNER_ID`,`ATTRIBUTE`, `VALUE`),
  CONSTRAINT `DATE_ANNO_OWNER_FK` FOREIGN KEY (`OWNER_ID`) REFERENCES `ANNOTATIONS_OWNER` (`OWNER_ID`) ON DELETE CASCADE
)
