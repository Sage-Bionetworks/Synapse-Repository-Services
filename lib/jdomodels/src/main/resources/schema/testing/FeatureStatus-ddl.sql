CREATE TABLE IF NOT EXISTS `FEATURE_STATUS` (
  `ID` BIGINT NOT NULL,
  `ETAG` char(36) NOT NULL,
  `FEATURE_TYPE` enum('DATA_ACCESS_AUTO_REVOCATION', 'DATA_ACCESS_NOTIFICATIONS', 'MULTIPART_AUTO_CLEANUP', 'DATA_DOWNLOAD_THROUGH_CLOUDFRONT', 'UPLOAD_LOCK_NOWAIT', 'CHANGE_PASSWORD_2FA_CHECK_BYPASS', 'ENFORCE_PROJECT_STORAGE_LIMITS', 'USE_NEW_ASYNC_GOOGLE_MULTIPART_UPLOAD', 'ALLOW_AGENT_WRITES', 'DISABLE_2FA_REQUIREMENT', 'DISABLE_INACTIVE_USERS') NOT NULL,
  `ENABLED` BOOLEAN NOT NULL,
  PRIMARY KEY (`ID`),
  UNIQUE KEY `FEATURE_TYPE` (`FEATURE_TYPE`)
)
