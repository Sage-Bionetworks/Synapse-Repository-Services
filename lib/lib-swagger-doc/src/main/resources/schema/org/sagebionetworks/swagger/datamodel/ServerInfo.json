{
	"description": "ServerInfo represents a server.",
	"properties": {
		"url": {
			"type": "string",
			"description": "A URL to the target host. Variable substitutions will be made when a variable is named in {brackets}."
		},
		"description": {
			"type": "string",
			"description": "An optional string describing the host designated by the URL."
		}
	}
}