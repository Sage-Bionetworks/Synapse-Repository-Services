{
	"description": "Represents the request body for an endpoint.",
	"properties": {
		"content": {
			"description": "Maps the media/content type of the request body to the expected value.",
			"type": "map",
		    "key": {
		    	"type": "string",
		    	"description": "A content type."
		    },
		    "value": {
		    	"$ref": "org.sagebionetworks.swagger.datamodel.pathinfo.Schema",
		    	"description": "The schema <this should be referencing a JSONSchema obj.>."
		    }
		},
		"required": {
			"description": "Represents if the request body is required",
			"type": "boolean"
		}
	}
}