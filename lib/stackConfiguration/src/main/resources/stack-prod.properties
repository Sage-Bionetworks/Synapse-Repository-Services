# Passwords and credentials do not belong in these files, pass them as VM args instead

# Prod-only properties belong in this file

# TODO right now we are saying the alpha stack is "prod" but later when we nuke the demo stack, we'll repurpose those cnames for prod

#---- Endpoint configuration overrides
org.sagebionetworks.portal.endpoint=https://synapse.sagebase.org
org.sagebionetworks.repositoryservice.endpoint=https://repositoryservice.sagebase.org/repo/v1

#---- S3 configuration
org.sagebionetworks.s3.bucket=data01.sagebase.org
org.sagebionetworks.s3.iam.group=DataUsers
