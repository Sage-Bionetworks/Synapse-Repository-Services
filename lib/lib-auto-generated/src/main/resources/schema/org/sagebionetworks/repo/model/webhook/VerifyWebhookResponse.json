{
	"description": "The request body of VerifyWebhookResponse.",
	"properties": {
		"webhookId": {
			"type": "string",
			"description": "The ID associated with the Webhook."
		},
		"isValid": {
			"type": "boolean",
			"description": "True if the webhook was successfully validated."
		},
		"invalidReason": {
			"type": "string",
			"description": "If the Webhook is invalid, this will contain a string describing why it is invalid. Otherwise, this will be null."
		}
	}
}
