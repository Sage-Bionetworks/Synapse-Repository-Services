{
	"description": "A view of Synapse content. Each view is defined with a container scope and a schema.",
	"type": "interface",
	"implements": [
		{
			"$ref":"org.sagebionetworks.repo.model.table.Table"
		}
	],
	"properties": {
		"scopeIds": {
		    "description":"The list of container IDs (projects or folders) that define the scope of this view.",
			"type": "array",
			"items": {
				"type": "string"
			}
		}
	}
}