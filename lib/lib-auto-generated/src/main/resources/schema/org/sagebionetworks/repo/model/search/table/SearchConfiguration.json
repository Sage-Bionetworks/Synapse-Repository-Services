{
	"description": "<p>Bundles the index-wide default analyzer and per-column overrides used to build a SearchIndex. A SearchConfiguration is the equivalent of the analyzer wiring in the <code>settings.analysis</code> block of an OpenSearch <a href='https://docs.opensearch.org/latest/api-reference/index-apis/create-index/'>create-index</a> request &mdash; it points at the TextAnalyzer that supplies <code>analysis.analyzer.default</code> (and optionally <code>analysis.analyzer.default_search</code>) for the index, then layers any per-column overrides on top. Bind one to a project (or any entity) and every SearchIndex below it inherits the configuration.</p><p><b>Reference vs inline.</b> Every analyzer / override slot accepts the same shape: either a reference to a saved resource &mdash; <code>{\"$ref\": \"{organizationName}-{name}\"}</code> &mdash; or an inline literal pasted directly. References preserve reuse and central editing; inline literals keep the configuration self-contained but cannot be referenced from elsewhere. The same <code>$ref</code> shape is also used inside a TextAnalyzer's <code>settings.filter</code> registry map to point at a SynonymSet, so curators learn one rule.</p><p>Example using a saved TextAnalyzer plus a saved override (both via <code>$ref</code>):</p><pre><code>{\n  \"organizationName\": \"biomed\",\n  \"name\": \"publications_v1\",\n  \"defaultAnalyzer\": { \"$ref\": \"org.sagebionetworks-SCIENTIFIC\" },\n  \"columnAnalyzerOverrides\": [\n    { \"$ref\": \"biomed-publications_overrides\" }\n  ]\n}</code></pre><p>Same shape, but with an inline override (the analyzer for the override's columns is itself a <code>$ref</code>):</p><pre><code>{\n  \"organizationName\": \"biomed\",\n  \"name\": \"publications_v1\",\n  \"defaultAnalyzer\": { \"$ref\": \"org.sagebionetworks-SCIENTIFIC\" },\n  \"columnAnalyzerOverrides\": [\n    {\n      \"overrides\": [\n        { \"columnName\": \"disease_code\", \"analyzer\": { \"$ref\": \"biomed-acronym_exact\" } },\n        { \"columnName\": \"authors\",      \"analyzer\": { \"$ref\": \"biomed-scientific_search\" } }\n      ]\n    }\n  ]\n}</code></pre><p><b>Asymmetric index/search analysis</b> (e.g. edge_ngram autocomplete) is expressed inside the chosen TextAnalyzer's settings JSON by declaring both <code>analyzer.default</code> and <code>analyzer.default_search</code>; OpenSearch picks the right one per its <a href='https://docs.opensearch.org/latest/analyzers/search-analyzers/'>search-analyzer precedence</a> rules. There is no separate search-time analyzer field on the SearchConfiguration &mdash; in line with the OpenSearch docs' guidance that this case is uncommon.</p><p><b>Synonyms</b> are not wired through the SearchConfiguration. A TextAnalyzer that wants synonyms references a SynonymSet directly via <code>{\"$ref\": \"{org}-{name}\"}</code> inside its own <code>settings.filter</code> registry map &mdash; see TextAnalyzer.</p><p>See SearchConfigBinding for how the configuration attaches to a project, TextAnalyzer / ColumnAnalyzerOverride for the analyzer components.</p>",
	"properties": {
		"id": {
			"type": "string",
			"description": "The unique ID of this search configuration."
		},
		"organizationName": {
			"type": "string",
			"description": "The name of the Organization this resource belongs to. Immutable after creation."
		},
		"name": {
			"type": "string",
			"description": "The resource name. Must start with a letter and contain only letters, digits, and underscores. Unique within the organization and immutable after creation. Used as part of the qualified name ({organizationName}-{name}) when referenced by other resources."
		},
		"description": {
			"type": "string",
			"description": "Optional description."
		},
		"defaultAnalyzer": {
			"type": "object",
			"description": "<p>Optional. The analyzer that supplies this index's <code>analysis.analyzer.default</code> slot. Either a reference to a saved TextAnalyzer (preferred &mdash; supports reuse) written as <code>{\"$ref\": \"{organizationName}-{name}\"}</code>, or an inline analyzer literal pasted directly. The <code>$ref</code> form looks up the row by qualified name; the inline form lives only inside this SearchConfiguration's JSON, has no persisted id, and cannot be reused elsewhere.</p><p><b>$ref form:</b></p><pre><code>\"defaultAnalyzer\": { \"$ref\": \"biomed-publications\" }</code></pre><p><b>Inline form</b> &mdash; the bare OpenSearch <code>settings.analysis</code> block. Allowed root keys are <code>char_filter</code>, <code>tokenizer</code>, <code>filter</code>, and <code>analyzer</code>. Refs to SynonymSets are not permitted inside an inline analyzer literal &mdash; save a TextAnalyzer and reference it by qualified name to use synonyms.</p><pre><code>\"defaultAnalyzer\": {\n  \"tokenizer\": { \"std\": { \"type\": \"standard\" } },\n  \"filter\":    { \"english_stop\": { \"type\": \"stop\", \"stopwords\": \"_english_\" } },\n  \"analyzer\": {\n    \"default\": {\n      \"type\": \"custom\",\n      \"tokenizer\": \"std\",\n      \"filter\": [\"lowercase\", \"english_stop\"]\n    }\n  }\n}</code></pre><p>If the analyzer also declares an <code>analyzer.default_search</code> entry, that becomes the index's <code>analysis.analyzer.default_search</code> &mdash; see OpenSearch's <a href='https://docs.opensearch.org/latest/analyzers/index-analyzers/'>index analyzers</a> and <a href='https://docs.opensearch.org/latest/analyzers/search-analyzers/'>search analyzers</a> for the precedence rules. If omitted, each column falls back to the system default analyzer for its data type (see ColumnTypeToOpenSearchMapping).</p>"
		},
		"columnAnalyzerOverrides": {
			"type": "array",
			"description": "<p>Optional ordered list of ColumnAnalyzerOverride entries. Each entry is either a reference written as <code>{\"$ref\": \"{organizationName}-{name}\"}</code> (preferred &mdash; supports reuse) or an inline ColumnAnalyzerOverride literal. Inline entries live only inside this SearchConfiguration's JSON. First match wins for duplicated column names across overrides.</p><p><b>$ref form:</b></p><pre><code>\"columnAnalyzerOverrides\": [\n  { \"$ref\": \"biomed-publications_overrides\" }\n]</code></pre><p><b>Inline form</b> &mdash; the same JSON shape a ColumnAnalyzerOverride record would have, minus identity / audit fields. The inner per-column <code>analyzer</code> slot still accepts either a <code>$ref</code> to a saved TextAnalyzer or a bare OpenSearch <code>settings.analysis</code> block:</p><pre><code>\"columnAnalyzerOverrides\": [\n  {\n    \"overrides\": [\n      { \"columnName\": \"disease_code\", \"analyzer\": { \"$ref\": \"biomed-acronym_exact\" } },\n      { \"columnName\": \"abstract\",     \"analyzer\": {\n        \"analyzer\": {\n          \"default\": { \"type\": \"custom\", \"tokenizer\": \"standard\", \"filter\": [\"lowercase\"] }\n        }\n      } }\n    ]\n  }\n]</code></pre>",
			"items": {
				"type": "object"
			}
		},
		"columnSemanticEnrichment": {
			"type": "array",
			"description": "<p>Optional list of ColumnSemanticEnrichmentEntry records that opt specific text columns in to AOSS Automatic Semantic Enrichment. At index-build time each entry causes its matching column's text-field mapping to be emitted with a <code>semantic_enrichment</code> block; AOSS then auto-attaches its managed sparse model to the ingest path and silently rewrites <code>match</code> queries (standalone or inside <code>bool</code> / <code>boosting</code> / <code>constant_score</code> / <code>dis_max</code> / <code>function_score</code> / <code>hybrid</code> compound queries) against the column into neural sparse queries. <code>multi_match</code> queries are NOT rewritten by AOSS, so a SearchIndexQuery that wants semantic matching against an enriched column must use <code>SearchQueryType.MATCH</code> (single field) rather than <code>MULTI_MATCH</code> &mdash; the existing search path on SearchManagementController / SearchQueryWorker carries this through transparently once the column is enriched.</p><p><b>Per-column opt-in only.</b> Unlike <code>defaultAnalyzer</code>, there is no <code>defaultSemanticEnrichment</code> on SearchConfiguration: AOSS meters semantic-search OCUs against the collection (soft cap of 100 TPS per the AWS docs) and bills per ingested token, so enriching every text column by default would inflate spend on every indexed row.</p><p><b>Example:</b></p><pre><code>\"columnSemanticEnrichment\": [\n  { \"columnName\": \"abstract\",    \"languageMode\": \"MULTI_LINGUAL\" },\n  { \"columnName\": \"description\", \"languageMode\": \"ENGLISH\" }\n]</code></pre><p>Entries whose <code>columnName</code> is not present (or is not a top-level text type) in a given SearchIndex's schema are silently skipped at index-build time with a warning surfaced through SearchIndexStatus.errorMessage, mirroring how columnAnalyzerOverrides handles missing columns. Per the AWS docs, AOSS Automatic Semantic Enrichment is restricted to top-level <code>text</code> fields &mdash; nested fields and STRING_LIST columns are not eligible.</p>",
			"items": {
				"type": "object"
			}
		},
		"etag": {
			"type": "string",
			"description": "Synapse employs an Optimistic Concurrency Control (OCC) scheme."
		},
		"createdOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date this resource was created."
		},
		"createdBy": {
			"type": "string",
			"description": "The ID of the user that created this resource."
		},
		"modifiedOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date this resource was last modified."
		},
		"modifiedBy": {
			"type": "string",
			"description": "The ID of the user that last modified this resource."
		}
	}
}
