{
	"description": "The upload destination contains information to start an upload to different types of destinations.",
	"type": "interface",
	"properties": {
        "concreteType": {
            "type": "string",
            "description": "Indicates which implementation this object represents."
        },
		"banner": {
			"type": "string",
			"description": "If set, the client should show this banner every time an upload is initiated"
		}
	}
}
