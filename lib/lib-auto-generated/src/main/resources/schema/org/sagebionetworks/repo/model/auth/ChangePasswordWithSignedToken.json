{
	"description": "Change user account's password by providing a signed token issued by the server",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.auth.ChangePasswordInterface"
		}
	],
	"properties": {
		"currentPassword": {
			"type": "string",
			"description": "The current password of the account"
		}
	}

}