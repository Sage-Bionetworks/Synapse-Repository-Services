{
	"description": "Request to list synonym sets, optionally filtered by organization.",
	"properties": {
		"organizationId": {
			"type": "string",
			"description": "Optional filter by organization ID."
		},
		"nextPageToken": {
			"type": "string",
			"description": "Token for the next page of results."
		}
	}
}
