{
	"description": "DTO for the webhook data that needs to be published to an endpoint",
	"properties": {
		"isVerificationMessage": {
			"type": "boolean"
		},
		"messageBody": {
			"type": "string"
		},
		"webhookId": {
			"type": "string"	
		},
		"endpoint": {
			"type": "string"
		}
	}
}