{
	"description": "Per-field options for a <a href=\"https://docs.opensearch.org/latest/query-dsl/term/prefix/\"><code>prefix</code></a> term-level clause. Carried as the value of the field-keyed <code>prefix</code> map (the map key is the column name).",
	"implements": [
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.ClauseScoringOptions" }
	],
	"properties": {
		"value": {
			"type": "object",
			"description": "Required. The prefix the indexed term must start with. A string (or scalar value on a non-text column)."
		},
		"case_insensitive": {
			"type": "boolean",
			"description": "Optional. When <code>true</code>, matches the prefix regardless of case. Default <code>false</code>."
		},
		"rewrite": {
			"type": "string",
			"description": "Optional. How the multi-term query is rewritten internally."
		}
	}
}
