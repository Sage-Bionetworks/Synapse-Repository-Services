{
    "title": "SortItem",
    "properties": {
        "column": {
            "type": "string",
            "description": "The column to sort on."
        },
        "direction": {
            "type": "string",
            "description": "Optional sort direction. Default is the default mysql sort direction for that type.",
            "name": "SortDirection",
            "id": "org.sagebionetworks.repo.model.table.SortDirection",
            "enum": [
                {
                    "name": "ASC",
                    "description": "Sort in ascending order (A-Z, 0-9)."
                },
                {
                    "name": "DESC",
                    "description": "Sort in descending order (Z-A, 9-0)."
                }
            ]
        }
    }
}