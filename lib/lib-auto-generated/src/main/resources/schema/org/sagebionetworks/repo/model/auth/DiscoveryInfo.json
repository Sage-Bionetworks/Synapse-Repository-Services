{
	"description": "JSON equivalent of the OpenID4Java library's DiscoveryInformation object",
	"properties": {
		"openIdEndpoint": {
			"type": "string",
			"description": "URL containing an OpenID provider's endpoint"
		},
		"identifier": {
			"type": "string",
			"description": "User's identity key"
		},
		"delegate": {
			"type": "string",
			"description": "Key through with the OpenID provider remembers the User's identity"
		},
		"version": {
			"type": "string",
			"description": "OpenID protocol version, or service type discovered via Yadis"
		},
		"serviceTypes": {
			"type": "array",
			"description": "All service types discovered for the endpoint", 
			"uniqueItems": true,
			"items": {
				"type": "string"
			}
		}
	}
}