{
	"description": "The upload destination list contains information to start an upload to different types of destinations.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.project.ProjectSetting"
		}
	],
	"properties": {
		"destinations": {
			"type": "array",
			"description": "the list of upload destination settings. The first one in the list is the default",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.project.UploadDestinationSetting"
			}
		},
		"locations": {
			"type": "array",
			"description": "the list of upload destination locations. The first one in the list is the default",
			"items": {
				"type": "integer",
				"description": "an upload id for an upload location"
			}
		}
	}
}
