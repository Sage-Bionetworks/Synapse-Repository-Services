{
	"description": "All entity types share a set of fields, like 'name', 'id', 'parentId' etc. To filter by these fields, add a condition of this type to a query.",
	"type": "object",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.entity.query.Condition"
		}
	],
    "properties":{
        "leftHandSide":{
            "$ref":"org.sagebionetworks.repo.model.entity.query.EntityFieldName"
        }
    }
}