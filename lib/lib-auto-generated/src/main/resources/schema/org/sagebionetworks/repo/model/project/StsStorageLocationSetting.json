{
    "description": "An StsStorageLocation points to an S3 bucket and base key, has the invariant that the Synapse folder hierarchy matches the S3 folder hierarchy, and you can call AWS Security Token Service (STS) to get temporary AWS credentials for the bucket and base key corresponding to the StsStorage Location.",
    "extends": {
        "$ref": "org.sagebionetworks.repo.model.project.ExternalS3StorageLocationSetting"
    }
}
