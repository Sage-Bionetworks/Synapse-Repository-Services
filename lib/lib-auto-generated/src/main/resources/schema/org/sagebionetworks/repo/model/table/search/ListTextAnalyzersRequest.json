{
	"description": "Request to list text analyzers. Returns system analyzers if no organizationId is provided.",
	"properties": {
		"organizationId": {
			"type": "string",
			"description": "The ID of the Organization to list analyzers for. If null, lists system analyzers."
		},
		"nextPageToken": {
			"type": "string",
			"description": "Optional pagination token from a previous response."
		}
	}
}
