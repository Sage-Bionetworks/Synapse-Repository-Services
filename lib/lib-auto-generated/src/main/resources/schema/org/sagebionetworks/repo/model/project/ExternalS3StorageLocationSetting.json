{
	"description": "The external upload destination setting contains information to generate an external S3 upload destination managed by Synapse.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.project.StorageLocationSetting"
		}
	],
	"properties": {
		"bucket": {
			"type": "string",
			"description": "the bucket to use"
		},
		"baseKey": {
			"type": "string",
			"description": "the optional base key to use"
		}
	}
}
