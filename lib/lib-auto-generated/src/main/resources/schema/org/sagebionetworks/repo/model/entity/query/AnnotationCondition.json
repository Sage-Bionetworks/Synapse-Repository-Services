{
	"description": "Custom annotations can be added to any entity.  To filter by these annotations add a condition of this type to a query.  Note: Annotations updates are eventually consistent.  So queries that include annotation conditions will also be eventually consistent.",
	"type": "object",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.entity.query.Condition"
		}
	],
    "properties":{
        "leftHandSide": {
			"type": "string",
			"description": "The name of the annotation to use as the left-hand-side of a query condition."
		}
    }
}