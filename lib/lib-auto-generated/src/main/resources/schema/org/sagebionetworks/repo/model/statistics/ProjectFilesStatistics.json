{
    "title":"Project File Statistics",
    "description":"Container for the statistics related to files in a project. Exposes the monthly aggregates of files and (unique) users that downloaded/uploaded for the past 12 months (excluding the current month).",
    "implements":[
        {
            "$ref":"org.sagebionetworks.repo.model.statistics.ObjectStatistics"
        }
    ],
    "properties":{
        "downloads": {
        	"type": "object",
        	"$ref": "org.sagebionetworks.repo.model.statistics.MonthlyFilesStatistics",
        	"description": "Contains the project file downloads statistics over the last 12 months. Excluded if the includeDownloads parameter is set to false."
        },
        "uploads": {
        	"type": "object",
        	"$ref": "org.sagebionetworks.repo.model.statistics.MonthlyFilesStatistics",
        	"description": "Contains the project file uplodas statistics over the last 12 months. Excluded if the includeUploads parameter is set to false."
        }
    }
}