{
	"description": "Results for a backup type job.",
	"implements":[
        {
            "$ref":"org.sagebionetworks.repo.model.migration.AdminResponse"
        },
		{
			"$ref": "org.sagebionetworks.repo.model.migration.HasMigrationType"
		}
    ],
	"properties": {
		"ranges": {
			"description": "List of object IDs to delete",
			"type": "array",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.migration.IdRange"
			}
		}
	}
}