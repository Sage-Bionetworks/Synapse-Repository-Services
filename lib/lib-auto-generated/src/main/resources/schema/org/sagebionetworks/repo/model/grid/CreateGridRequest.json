{
	"description": "Start a job to create a new Grid session.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.asynch.AsynchronousRequestBody"
		}
	],
	"properties": {
		"initialQuery": {
			"$ref": "org.sagebionetworks.repo.model.table.Query",
			"description": "When provided, the grid will be initialized using the provided query against a table or view.  The grid columns will match the query select statement.  One row will be added to the grid for each row returned by the query. Optional."
		},
		"schema$id": {
			"type": "string",
			"description": "The $id of the JSON schema to be used for model validation.  Note: If the $id includes a semantic version then specific version will be used.  If the $id excludes the semantic version then the latest version of that schema."
		}
	}
}