{
	"description": "A token returned to the client when two factor authentication is required.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.SignedTokenInterface"
		}
	],
	"properties": {
		"userId": {
			"type": "integer",
			"description": "The id of the user."
		},
		"context": {
			"$ref": "org.sagebionetworks.repo.model.auth.TwoFactorAuthTokenContext"
		},
		"identityProvider": {
			"$ref": "org.sagebionetworks.repo.model.auth.IdentityProvider",
			"description": "The identity provider that authenticated the login attempt requiring the second factor. Absent when the token was issued for a context other than authentication."
		}
	}
}