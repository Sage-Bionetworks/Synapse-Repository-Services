{
	"description": "In expression is used for a where condition using the 'in' opperator.",
	"type": "object",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.grid.sql.SqlExpression"
		}
	],
	"properties": {
		"left": {
			"$ref": "org.sagebionetworks.repo.model.grid.sql.ColumnReference",
			"description": "The left-hand side expression."
		},
		"values": {
			"type": "array",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.grid.sql.Literal"
			},
			"description": "An array of literal values for the 'IN' operator."
		},
		"not": {
			"type": "boolean",
			"description": "Optional (default to false). Indicates if it's a 'NOT IN' expression."
		}
	}
}
