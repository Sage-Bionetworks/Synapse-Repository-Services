{
	"description": "Information needed to create a new account or add a new email address to an existing account.",
	"properties": {
		"emailValidationToken": {
			"type": "string",
			"description": "Deprecated. The validation token sent via email"
		},
		"emailValidationSignedToken": {
			"type": "object",
			"description": "Signed token containing the email address to be added.",
			"$ref": "org.sagebionetworks.repo.model.principal.EmailValidationSignedToken"
		}
	}
}