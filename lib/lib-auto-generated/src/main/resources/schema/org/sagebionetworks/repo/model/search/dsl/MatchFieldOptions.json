{
	"description": "Per-field options for a <a href=\"https://docs.opensearch.org/latest/query-dsl/full-text/match/\"><code>match</code></a> full-text clause. Carried as the value of the field-keyed <code>match</code> map (the map key is the column name).",
	"implements": [
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.ClauseScoringOptions" },
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.FuzzyMatchOptions" },
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.MinimumShouldMatchOption" },
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.ZeroTermsQueryOption" }
	],
	"properties": {
		"query": {
			"type": "object",
			"description": "Required. The text (or scalar value on a non-text column) to match. A string, number, or boolean depending on the target column type."
		},
		"operator": {
			"type": "string",
			"description": "Optional. Boolean logic used to combine the analyzed query terms: <code>or</code> (default) or <code>and</code>."
		},
		"max_expansions": {
			"type": "integer",
			"description": "Optional. Maximum number of terms the fuzzy expansion will generate."
		},
		"cutoff_frequency": {
			"type": "number",
			"description": "Optional. Term-frequency threshold above which terms are treated as low-importance."
		},
		"auto_generate_synonyms_phrase_query": {
			"type": "boolean",
			"description": "Optional. Whether to auto-generate phrase queries for multi-term synonyms. Default <code>true</code>."
		},
		"lenient": {
			"type": "boolean",
			"description": "Optional. When <code>true</code>, format-based errors (e.g. a text value against a numeric field) are ignored."
		}
	}
}
