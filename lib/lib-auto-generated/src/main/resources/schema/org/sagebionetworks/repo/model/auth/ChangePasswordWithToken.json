{
	"description": "Change user account's password by providing a token issued by the server",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.auth.ChangePasswordInterface"
		}
	],
	"properties": {
		"passwordChangeToken": {
			"$ref": "org.sagebionetworks.repo.model.auth.PasswordResetSignedToken",
			"description": "A token provided by the server which authorizes a password change"
		}
	}

}