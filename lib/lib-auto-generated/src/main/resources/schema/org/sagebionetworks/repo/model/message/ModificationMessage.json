{
	"title": "ModificationMessage",
	"description": "JSON schema for an object modification message",
	"properties": {
		"projectId": {
			"type": "integer",
			"description":"The project that changed."
		},
		"entityId": {
			"type": "string",
			"description":"The entity that changed."
		},
		"userId": {
			"type": "integer",
			"description":"The user id for which the change applies"
		},
		"timestamp": {
			"type": "string",
			"format":"date-time",
			"description":"The timestamp when this modification was originally committed."
		}
	}
}
