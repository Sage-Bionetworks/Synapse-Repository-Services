{
	"description": "The upload destinations setting contains a list of possible upload destinations.",
	"type": "interface",
	"properties": {
        "concreteType": {
            "type": "string",
            "description": "Indicates which implementation this object represents."
        },
		"banner": {
			"type": "string",
			"description": "The optional banner to show every time a file is uploaded"
		}
	}
}
