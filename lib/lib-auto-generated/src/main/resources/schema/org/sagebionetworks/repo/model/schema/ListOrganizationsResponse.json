{
	"description": "A single page of the currently registered Organizations",
	"properties": {
		"page": {
			"type": "array",
			"items":{
				"$ref" : "org.sagebionetworks.repo.model.schema.Organization"
			}
		},
		"nextPageToken": {
			"type": "string",
			"description": "Forward this token to get the next page of results."
		}
	}
}
