{
	"description": "SQL used to update cells of a grid.",
	"type": "object",
	"properties": {
		"set": {
			"type": "array",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.grid.sql.SetValue"
			},
			"description": "Values to set."
		},
		"where": {
			"type": "array",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.grid.sql.SqlExpression"
			},
			"description": "Optional WHERE clause."
		},
		"limit": {
			"type": "integer",
			"description": "The number of rows to limit."
		}
	}
}
