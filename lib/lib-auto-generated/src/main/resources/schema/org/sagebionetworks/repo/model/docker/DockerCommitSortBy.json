{
    "type": "string",
    "enum": [
        {
            "name": "CREATED_ON",
            "description": "Sort Docker commits by the date they were created."
        },
        {
            "name": "TAG",
            "description": "Sort Docker commits by their tag value."
        }
    ]
}