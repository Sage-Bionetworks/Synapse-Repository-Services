{
	"description": "An implementation of LocalStackMessage that contains a webhookId. This will be used to push messages related to the webhook onto our asynchronous worker queues.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.message.LocalStackMessage"
		}
	],
	"properties": {
		"webhookId": {
			"type": "string",
			"description": "The ID associated with the Webhook."
		}
	}
}
