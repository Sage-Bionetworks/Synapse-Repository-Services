{
	"description": "A Cost Report CSV download request.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.asynch.AsynchronousRequestBody"
		}
	],
	"properties": {
		"reportType": {
			"name": "CostReportType",
			"type": "string",
			"description": "The type of cost report to generate. The default is \"all_projects\"",
			"enum": [
				{
					"name": "all_projects",
					"description": "Generates a report including all projects."
				}
			]
		}
	}
}
