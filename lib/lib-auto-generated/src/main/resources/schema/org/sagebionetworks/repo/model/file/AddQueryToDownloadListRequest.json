{
	"description": "Request to add the files from the given view query to a user's download list.  Note: A user's download list can only include 100 files or less.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.file.AddFileToDownloadListRequest"
		}
	],
	"properties": {
		"query": {
			"$ref": "org.sagebionetworks.repo.model.table.Query",
			"description": "A view query that lists the files to be added to a user's download list."
		}
	}
}
