{
	"description": "Request to add the files contained within the given folder ID to a user's download list. Note: A user's download list can only include 100 files or less.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.file.AddFileToDownloadListRequest"
		}
	],
	"properties": {
		"folderId": {
			"type": "string",
			"description": "The ID of the folder containing the files to add to the user's download list. Note: Only the files in this folder will be added to the download list, files in sub-folders will not be added."
		}
	}
}
