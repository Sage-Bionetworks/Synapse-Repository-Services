{
	"description": "A request to change the state of a DataAccessSubmission.",
	"properties": {
		"submissionId":{
			"type": "string",
			"description": "The ID of the requested DataAccessSubmission."
		},
		"newState": {
			"$ref":"org.sagebionetworks.repo.model.dataaccess.DataAccessSubmissionState",
			"description": "The new status to set to the requested DataAccessSubmission."
		},
		"rejectedReason":{
			"type": "string",
			"description": "The reason that the DataAccessSubmission is rejected if newState is REJECTED."
		}
	}
}