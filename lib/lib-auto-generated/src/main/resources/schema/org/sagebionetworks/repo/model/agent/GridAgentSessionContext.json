{
	"description": "Additional context provided for grid agent chat sessions.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.agent.SessionContext"
		}
	],
	"properties": {
		"gridSessionId": {
			"description": "Required. The grid session ID.",
			"type": "string"
		},
		"usersReplicaId": {
			"description": "Required. The calling user's grid replica ID.",
			"type": "integer"
		},
		"agentsReplicaId": {
			"description": "The replica ID issued to the user's agent.",
			"type": "integer"
		},
		"experimental": {
			"description": "Optional. When true, this grid session is processed by the experimental Curie multi-agent supervisor instead of the default agent. Default value is false.",
			"type": "boolean"
		}
	}
}
