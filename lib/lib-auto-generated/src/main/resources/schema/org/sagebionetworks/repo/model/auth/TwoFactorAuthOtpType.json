{
  "type": "string",
  "description": "The type of one time password code that can be used to authenticate through two factor authentication.",
  "enum": [
  	{
		"name": "TOTP",
		"description": "A time-based one time password that is generated by authenticator applications. Default to this if not present."
	},
	{
		"name": "RECOVERY_CODE",
		"description": "A single use code used as a backup for 2FA"
	}
  ]
}