{
	"title": "Has Two Factor Auth Requirement",
	"type": "interface",
	"description": "Describes an Access Requirement that may require an accessor to enable two factor authentication.",
	"properties": {
		"isTwoFaRequired": {
			"type": "boolean",
			"description": "If true, then accessor needs to enable two factor authentication before gaining access to the data."
		}
	}
}
